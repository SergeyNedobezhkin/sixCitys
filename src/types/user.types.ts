export type UserTypes = {
  name: string;
  isPro: boolean;
  avatarUrl: string;
}

export type UserData = {
  id: number,
  email: string,
  token: string
}
export type AuthData = {
  login: string,
  password: string
}